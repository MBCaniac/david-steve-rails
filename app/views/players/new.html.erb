<h1>
  Create a new player
</h1>

<%= form_with model: @player do |f| %>
  <% @player.errors.full_messages.each do |message| %>
      <div><%= message %></div>
  <% end %>
  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>